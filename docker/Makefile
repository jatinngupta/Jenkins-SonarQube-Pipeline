build:
	wget https://updates.jenkins-ci.org/latest/jenkins.war
	docker build -t jenkins-docker  .

#run:
#	docker run -d -p 8080:8080 -p 50000:50000 -v ${PWD}:/var/jenkins_home -v /var/run/docker.sock:/var/run/docker.sock jenkins-docker

